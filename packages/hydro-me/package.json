{
  "name": "@elemental-sea/hydro-me",
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
		"sns-post": "bun run src/index.ts",
		"sns-post:dev": "bun run src/index.ts --watch",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "bunx @biomejs/biome lint .",
    "format": "bunx @biomejs/biome format . --write",
    "format-check": "bunx @biomejs/biome format .",
    "type-check": "tsc"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.4",
    "@vitest/coverage-v8": "^1.5.0",
    "@elemental-sea/tsconfig": "workspace:*",
    "typescript": "^5.4.4",
    "vitest": "^1.5.0",
    "@elemental-sea/vitest-config": "workspace:*"
  },
  "dependencies": {
    "langchain": "^0.1.34",
    "langsmith": "^0.1.14"
  },
  "overrides": {
    "@langchain/core": "0.1.58"
  }
}
