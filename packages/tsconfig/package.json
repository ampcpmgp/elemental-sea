{
  "name": "@elemental-sea/tsconfig",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo 'Add test script here'",
    "lint": "echo 'Add lint script here'",
    "format": "bunx @biomejs/biome format . --write",
    "format-check": "bunx @biomejs/biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.7.0",
    "@tsconfig/recommended": "^1.0.6",
    "vitest": "^1.5.0"
  }
}
